#pragma once

#include <stdlib.h>
#include <stdbool.h>
#include <stdint.h>

typedef uint8_t octet;
typedef uint8_t nb_ports;


typedef struct IP{
    octet address[4]

} IP;

typedef struct MAC{
    octet address[6]
} MAC;


//!Rajouter les fonctions dedans.
